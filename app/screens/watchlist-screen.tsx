import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

type Props = {};

const WatchlistScreen = (props: Props) => {
  return (
    <View>
      <Text>WatchlistScreen</Text>
    </View>
  );
};

export default WatchlistScreen;

const styles = StyleSheet.create({});
